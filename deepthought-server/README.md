# deepthought-server

Scaffold for the server-side product. No code yet — this directory exists so the
two-product repo layout is stable from day one.

When it gets a `CLAUDE.md`, the root `CLAUDE.md` and `CHANGELOG.md` conventions
apply to it exactly as they do to `deepthought-cli`.
