apiVersion: v1
kind: Service
metadata:
  name: deepthought-server
  namespace: deepthought
  labels:
    app: deepthought-server
spec:
  type: ClusterIP
  selector:
    app: deepthought-server
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
