package tui

// The hub screen was removed: chat is now the navigation root (home), F-keys and
// slash commands launch screens/overlays, and esc pops the screen history back
// to chat (see internal/app/model.go screenStack + BackMsg). This file is
// intentionally empty; delete it when convenient (rm was blocked by the shell
// auto-classifier).
