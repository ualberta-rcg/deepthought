package tui

// The standalone Cluster screen was folded into the unified Status page (see
// status.go, F12) — Slurm metrics now render as one Status section fed by the
// root's background poller. This file is intentionally empty; delete it when
// convenient (rm was blocked by the shell auto-classifier).
